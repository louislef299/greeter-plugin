syntax = "proto3";
package tutorial;

option go_package = "github.com/louislef299/greeter-plugin/api";

service Greeter {
    rpc Greet(Person) returns (Greeting);
}

message Person {
  string name = 1;
}

message Greeting {
  string message = 1;
}
